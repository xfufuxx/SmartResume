# 智能简历优化系统 — 功能概述

> 最后更新：2026-06-19

---

## 一、项目概述

智能简历优化系统是一个基于 AI 大语言模型的简历优化工具，帮助求职者根据目标岗位需求优化简历内容，提升人岗匹配度，并以多种排版方案输出专业 PDF。

**核心价值**：
- AI 解析简历 + 岗位，自动匹配分析
- 智能改写简历内容，突出与岗位相关的经验和技能
- 支持 4 种 PDF 输出方案（灵活可选）
- 异步任务处理，支持大文件和大规模优化

---

## 二、技术栈

### 后端（Python）

| 层级 | 技术 |
|------|------|
| Web 框架 | FastAPI |
| Python 版本 | 3.11+ |
| 数据库 ORM | SQLAlchemy 2.0 (async) |
| 数据库 | PostgreSQL (asyncpg) |
| 缓存 / 任务状态 | Redis |
| AI 模型 | OpenAI 兼容 API（默认 gpt-4o / gpt-4o-mini） |
| 认证 | JWT（access token + refresh token 双 token） |
| 密码加密 | passlib (bcrypt) |
| 邮件 | SMTP（QQ邮箱） |
| PDF 方案 1 | **PyMuPDF (fitz)** — 保留原样式的原地编辑 |
| PDF 方案 2 | **xelatex (MiKTeX/TeX Live)** — LaTeX 专业排版 |
| PDF 方案 3 | **Playwright (Chromium)** — HTML → PDF |
| PDF 方案 4 | **Jinja2 + WeasyPrint / ReportLab** — 固定模板回退 |
| 文件处理 | Pillow, python-docx, PyMuPDF |
| 文件存储 | 本地文件系统 (uploads/) |
| 数据库迁移 | Alembic |
| 依赖管理 | pip (requirements.txt) |

### 前端（TypeScript/React）

| 层级 | 技术 |
|------|------|
| 框架 | Next.js 14 (App Router) |
| 语言 | TypeScript 5.4+ |
| UI 组件库 | Ant Design 5 |
| 图标 | @ant-design/icons |
| 图表 | ECharts (echarts-for-react) |
| 文本差异对比 | diff |
| 文件拖拽上传 | react-dropzone |
| HTTP 客户端 | Axios |
| 日期处理 | dayjs |

---

## 三、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                      前端 (Next.js 14)                        │
│  ┌─────────┐ ┌──────────┐ ┌────────┐ ┌──────────┐         │
│  │  首页    │ │ 简历库    │ │ 岗位库  │ │ 个人中心  │         │
│  │ (上传/   │ │ (管理/    │ │ (管理/  │ │ (信息/    │         │
│  │  优化)   │ │  解析)   │ │  解析)  │ │  saved_  │         │
│  │          │ │          │ │        │ │  texts)  │         │
│  ├─────────┤ ├──────────┤ ├────────┤ ├──────────┤         │
│  │ 历史记录 │ │ 历史详情  │ │ 管理员  │ │ 仪表盘    │         │
│  └─────────┘ └──────────┘ └────────┘ └──────────┘         │
│                         │ HTTP/JSON                        │
├─────────────────────────┼──────────────────────────────────┤
│                  FastAPI 后端 (Python)                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  API 路由层 (12 个模块)                               │  │
│  │  auth / user / resumes / jobs / optimize             │  │
│  │  feedback / matching / scoring / batch / refine      │  │
│  │  messages / admin                                    │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │  服务层 (services/)                                  │  │
│  │  ┌──────────┐ ┌──────────┐ ┌─────────────────┐      │  │
│  │  │ resume   │ │ job      │ │ agent_optimizer  │      │  │
│  │  │ _parser  │ │ _parser  │ │ (匹配+优化引擎)   │      │  │
│  │  ├──────────┤ ├──────────┤ ├─────────────────┤      │  │
│  │  │ pdf_     │ │ resume   │ │ pdf_generator    │      │  │
│  │  │ styler   │ │ _renderer│ │ (Jinja2回退)     │      │  │
│  │  │ (策略层) │ │ (LaTeX/  │ └─────────────────┘      │  │
│  │  │          │ │  HTML)   │ ┌─────────────────┐      │  │
│  │  │          │ │          │ │ text_optimizer   │      │  │
│  │  │          │ │          │ └─────────────────┘      │  │
│  │  └──────────┘ └──────────┘ └─────────────────┘      │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │  数据层 (models/) → PostgreSQL + Redis               │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 核心数据流

```
用户上传简历+岗位 → AI解析 → 匹配分析 → 内容优化 → PDF生成 → 前端展示
                                            ↓
                                   四种方案可选（用户下拉选择）
```

---

## 四、核心功能详解

### 4.1 用户认证与个人中心

- **注册/登录**：邮箱注册 + 验证码，JWT 双 token 认证（access token 2h + refresh token 7d）
- **忘记密码**：邮箱验证码重置密码
- **个人中心**（`/profile`）：
  - 编辑个人信息（昵称、头像、求职状态）
  - **保存简历文本和岗位文本**（`saved_texts` 字段 JSONB）
  - 设置求职意向（`expectation` 字段 JSONB：行业、岗位、薪资、城市）
  - 设备管理（多设备登录追踪、设备吊销）
  - 消息通知
  - 数据导出、账号注销、隐私协议同意

### 4.2 简历管理

- **上传简历**：支持 PDF / PNG / JPG / WebP / DOCX（最大 20MB）
- **简历解析**：AI 解析为结构化 JSON（基本信息、工作经历、教育背景、技能、项目经历、自我评价等）
- **简历库**（`/resumes`）：
  - 列表查看、搜索、详情
  - 设为默认简历（`is_primary`）
  - 软删除
  - 优化时可直接从已有简历中选择
- 纯文本解析：`parse_resume_text()` 直接解析文本内容（一键优化使用）

### 4.3 岗位管理

- **上传岗位截图**：支持 JPG / PNG / WebP
- **岗位解析**：AI 提取职位名称、公司、薪资范围、地点、必备技能、加分项、福利等
- **岗位库**（`/jobs`）：
  - 列表查看、搜索、分类筛选
  - 收藏、软删除、恢复
  - 批量操作（批量删除、批量移至回收站）
  - 设置默认岗位、复制岗位
  - 优化时可直接从已有岗位中选择
- 纯文本解析：`parse_job_text()` 直接解析文本内容（一键优化使用）

### 4.4 简历优化（核心流程）

```
用户上传简历 + 岗位（或使用个人中心文本）
       │
       ▼
┌──────────────────────┐
│ 1. 解析阶段           │
│    PDF/图片/DOCX → AI → 结构化 JSON     │
│    或 parse_resume_text() / parse_job_text()
└──────┬───────────────┘
       ▼
┌──────────────────────┐
│ 2. 匹配分析           │
│    AI 分析简历与岗位的匹配度              │
│    输出: match_score / strengths / gaps  │
│          rewrite_strategy                │
└──────┬───────────────┘
       ▼
┌──────────────────────┐
│ 3. 内容优化           │
│    AI 根据匹配分析结果改写简历            │
│    输出: 优化后的结构化 JSON              │
│    约束: 不捏造技能/职位                  │
└──────┬───────────────┘
       ▼
┌──────────────────────┐
│ 4. 修改说明           │
│    AI 生成优化前后的差异说明文本           │
└──────┬───────────────┘
       ▼
┌──────────────────────┐
│ 5. PDF 生成（四选一）  │
│    ┌────────────────┐ │
│    │ 保留原样式      │ │  ← PyMuPDF 原地编辑（仅 PDF 文件）
│    │ LaTeX 专业排版  │ │  ← xelatex + ctexart（仅文件上传）
│    │ HTML+Playwright │ │  ← 统一 HTML 模板渲染
│    │ 专业分栏/简约单栏│ │  ← Jinja2 + WeasyPrint（通用回退）
│    └────────────────┘ │
└───────────────────────┘
```

#### 两种优化入口

| 入口 | 说明 | API 端点 |
|------|------|---------|
| **文件上传优化** | 上传简历文件 + 岗位截图，或从简历库/岗位库选择 | `POST /api/optimize/async` |
| **一键优化（纯文本）** | 使用个人中心 `saved_texts` 中的简历文本和岗位文本直接优化 | `POST /api/optimize/quick` |

#### 异步任务模式

提交优化任务后立即返回 `task_id`，前端轮询 `GET /api/optimize/async/{task_id}` 获取进度：
```
pending → processing → completed / failed
```
- 轮询间隔：2 秒
- 最大等待：5 分钟
- Redis 保留任务状态：1 小时
- 速率限制：每用户每分钟最多 3 次

### 4.5 四种 PDF 输出方案（7 种风格可选）

| 方案 | 技术 | 风格 | 适用场景 |
|------|------|------|---------|
| **保留原样式** | PyMuPDF 原地编辑 | 保留原 PDF 矢量图形、字体、颜色、照片 | 仅 PDF 文件上传 |
| **LaTeX 专业排版** | xelatex + ctexart + tikz | 深蓝侧边栏，专业排版引擎 | 仅文件上传（需安装 xelatex） |
| **HTML+Playwright** | 视觉模型 + HTML 模板 + Chromium | 深蓝侧边栏 + 右侧主内容双栏 | 文件上传（PDF/图片均可） |
| **Jinja2 模板（5 种）** | Jinja2 + WeasyPrint | 专业分栏 / 简约单栏 / 现代渐变 / 紧凑高效 / 优雅金边 | 通用（上传 / 一键优化） |

**Jinja2 模板详情**：

| 模板 | 文件 | 设计特点 |
|------|------|---------|
| 专业分栏 | `professional.html` | 蓝色主题，左侧联系信息 + 右侧工作经历，双栏布局 |
| 简约单栏 | `simple.html` | 极简风格，居中单栏，浅灰色调，方点列表 |
| 现代渐变 | `modern.html` | 蓝色渐变顶部横幅，左侧技能标签 + 右侧工作经历，圆角标签 |
| 紧凑高效 | `compact.html` | 最大信息密度，时间线排版，适合内容较多的简历 |
| 优雅金边 | `elegant.html` | 深色侧边栏 + 金色点缀，高端商务风格 |

> 用户可在「优化我的简历」按钮的 **Dropdown.Button** 下拉菜单中自由选择方案。
> 一键优化（纯文本）选择「保留原样式」或「LaTeX」方案时自动回退到「专业分栏」。
> 4 层策略由 `pdf_styler.py` 中的 `_generate_styled_or_fallback()` 统一路由。

### 4.6 优化结果展示

- **匹配度评分**：圆形进度条展示 0-100 分
- **匹配分析详情**：展示 strengths（优势）和 gaps（差距）
- **修改前后对比**：使用 `diff` 库进行文本级差异对比，绿色 = 新增，红色 = 删除
- **PDF 下载**：优化后的 PDF 可直接下载
- **二次优化（Refine）**：对已有优化结果再次优化（支持指定修改目标）

### 4.7 历史记录

- 所有优化记录列表（`/history`），支持搜索关键词 + 分类筛选
- 历史详情页：查看优化结果、匹配度、修改说明
- 收藏标记（`is_favorite`）
- 软删除（移至回收站）/ 恢复 / 永久删除
- 满意度评分（1-5 分）+ 反馈文字
- 不同版本对比（diff 端点）
- 支持分类：产品 / 开发 / 运营 / 设计 / 市场 / 销售 / 其他

### 4.8 仪表盘

- ECharts 展示优化统计
- 优化次数、匹配度趋势、行业分布等可视化数据
- 独立路由 `/dashboard`

### 4.9 管理员功能

- 独立管理员登录
- 用户管理（搜索、封禁/解封、VIP 设置、配额管理）
- 系统仪表盘（核心指标、行业分布、匹配改进、功能使用率、岗位趋势、满意度）
- 模板管理（创建/编辑/启用/停用 HTML+CSS 简历模板）
- 关键词管理（行业关键词、批量导入）
- ATS 规则管理
- Prompt 管理（版本管理、灰度发布、回滚）
- 模型管理（权重配置、速率限制、调用日志）
- 任务管理（失败重试）
- 订单 / 套餐 / 营收管理
- 反馈管理（聚类分析）
- 工单系统
- 操作日志

### 4.10 消息通知

- 系统消息列表
- 已读/未读管理
- 一键全部已读

---

## 五、数据库模型

### 模型关系

```
users (用户)
  ├── resumes (简历)          1:N
  ├── job_images (岗位截图)    1:N
  ├── optimized_resumes (优化) 1:N
  │     └── optimized_resumes (refine 链，自引用)
  ├── user_devices (设备)      1:N
  ├── messages (消息)          1:N
  ├── user_quotas (配额)       1:1
  ├── resume_templates (模板)  N:1
  └── audit_logs (审计日志)
```

### 关键模型字段

**User**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| email | String(255) | 邮箱（唯一索引） |
| phone | String(128) | 手机号（唯一） |
| password_hash | String(255) | bcrypt 哈希 |
| nickname | String(64) | 昵称 |
| avatar_url | String(1024) | 头像 URL |
| career_state | String(32) | 求职状态 |
| expectation | JSONB | 求职意向（行业/岗位/薪资/城市） |
| saved_texts | JSONB | 保存的简历和岗位文本 |
| status | String(16) | active / banned / deleted |

**OptimizedResume**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID 主键 |
| resume_id | String(36) | 关联简历（Nullable，一键优化可为空） |
| job_image_id | String(36) | 关联岗位（Nullable） |
| original_json | JSONB | 原始简历结构化 JSON |
| optimized_json | JSONB | 优化后简历结构化 JSON |
| match_score | Integer | 匹配度评分 0-100 |
| pdf_url | String(1024) | 生成的 PDF 下载地址 |
| changes_description | Text | AI 生成的修改说明 |
| job_title / company | String | 岗位名称 / 公司名 |
| category | String(50) | 分类（开发/产品/运营…） |
| is_favorite | Boolean | 收藏标记 |
| satisfaction_score | Integer | 满意度评分 1-5 |
| feedback_text | Text | 反馈文字 |
| parent_record_id | String(36) | 父记录 ID（refine 链） |
| refine_count | Integer | 优化迭代次数 |
| status | String(20) | completed / pending / failed |

---

## 六、API 路由总览

| 前缀 | 主要端点 | 功能 |
|------|---------|------|
| `/api/auth` | register, login, refresh, logout, send-code, forgot-password, change-password, devices, bind/unbind phone, delete-account | 认证 |
| `/api/user` | profile (GET/PUT), expectation, avatar, sync-saved-texts, saved-texts, privacy, export-data | 用户资料 |
| `/api/messages` | list, {id}/read, read-all, {id}/delete | 消息通知 |
| `/api/resumes` | upload, list, {id}, create, update, set-primary, delete, batch-delete | 简历管理 |
| `/api/jobs` | upload, list, {id}, update, set-primary, favorite, copy, trash, restore, favorites, categories, delete, batch-* | 岗位管理 |
| `/api/optimize` | /, /async, /async/{task_id}, /quick, /list, /{opt_id}, /{opt_id}/favorite, /{opt_id}/trash, /{opt_id}/restore, /{opt_id}/feedback, /favorites, /trash, /categories, /diff | 简历优化 |
| `/api/feedback` | /, list, stats | 满意度反馈 |
| `/api/match` | / | 单独匹配分析 |
| `/api/resume/{id}/score` | /score | 评分 |
| `/api/batch-optimize` | /, /{batch_id}/status | 批量优化 |
| `/api/optimization/{id}/refine` | /refine | 二次优化 |
| `/api/admin` | login, users/*, dashboard/*, templates/*, keywords/*, ats-rules/*, prompts/*, models/*, tasks/*, orders/*, packages/*, feedbacks/*, tickets/*, quota, admin-logs | 管理后台 |
| `/health` | / | 健康检查 |

---

## 七、AI 提示词模块

| 提示词文件 | 用途 | AI 角色 |
|-----------|------|---------|
| `resume_parse.py` | 将简历文本/图片解析为结构化 JSON | 简历解析专家 |
| `job_parse.py` | 将岗位描述/截图解析为结构化 JSON | 岗位分析专家 |
| `match_analysis.py` | 分析简历与岗位的匹配度，输出评分/优势/差距/改写策略 | 职业咨询师 |
| `optimize.py` | 根据匹配分析结果改写简历 JSON | 简历优化专家 |

所有提示词均要求 AI **仅返回 JSON**，不包含任何解释性文字，以便后端直接解析。

---

## 八、前端页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/login` | 登录页 | 邮箱密码登录 + 注册 |
| `/` | 首页 | 上传简历+岗位、一键优化、结果展示 |
| `/profile` | 个人中心 | 信息编辑、saved_texts、求职意向 |
| `/resumes` | 简历库 | 管理已上传简历 |
| `/jobs` | 岗位库 | 管理已上传岗位 |
| `/history` | 优化历史 | 所有优化记录列表 |
| `/history/[id]` | 历史详情 | 单条优化详情 |
| `/dashboard` | 仪表盘 | ECharts 统计 |
| `/admin/login` | 管理员登录 | 独立登录 |
| `/admin` | 管理后台 | 用户/模板/模型/订单等管理 |

---

## 九、配置项说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `DATABASE_URL` | postgresql+asyncpg://... | PostgreSQL 连接 |
| `REDIS_URL` | redis://localhost:6379/0 | Redis 连接 |
| `LLM_API_KEY` | — | AI API Key（必填） |
| `LLM_API_BASE` | — | 自定义 API 地址（可选） |
| `LLM_MODEL_VISION` | gpt-4o | 视觉模型（图片解析） |
| `LLM_MODEL_TEXT` | gpt-4o-mini | 文本模型（解析/优化） |
| `USE_WEASYPRINT` | true | 是否使用 WeasyPrint 渲染 |
| `USE_TEMPLATE_PDF` | true | 是否使用统一模板 |
| `MAX_UPLOAD_SIZE` | 20MB | 上传文件大小限制 |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | 120 | Token 过期时间 |
| `REFRESH_TOKEN_EXPIRE_DAYS` | 7 | Refresh token 过期时间 |
| `CORS_ORIGINS` | localhost:3000 | 跨域允许来源 |
| `SMTP_*` | QQ 邮箱 | 邮件发送配置 |

---

## 十、PDF 生成策略详解

`pdf_styler.py` 中的 `_generate_styled_or_fallback()` 是 PDF 生成的唯一入口，按以下优先级路由：

```
用户指定 template 参数？
  ├── 是 → 直接使用指定方案，失败回退到 Jinja2 专业分栏
  └── 否 → 按文件类型自动选择：

PDF 文件：
  ① PyMuPDF 原地编辑（保留矢量样式、字体、照片）→ 成功则返回
  ② LaTeX 编译（xelatex + ctexart）→ 成功则返回
  ③ HTML+Playwright（视觉模型+HTML模板）→ 成功则返回
  ④ Jinja2 + WeasyPrint 固定模板 → 最终回退

图片文件（PNG/JPG）：
  ① 视觉模型提取 → HTML+Playwright → 成功则返回
  ② Jinja2 + WeasyPrint 固定模板 → 最终回退

DOCX 文件：
  ① python-docx 替换 → 转 PDF → 成功则返回
  ② Jinja2 + WeasyPrint 固定模板 → 最终回退

纯文本（一键优化）：
  ① 根据 template 参数选择专业分栏/简约单栏
  ② 指定不支持方案时自动回退到专业分栏
```

---

## 十一、Git 提交历史（最新）

- Commit `bf16e9e`: `feat: 核心功能重大更新 - 多种输出方案/一键优化/个人中心/岗位库`
- 远程仓库：`https://github.com/xfufuxx/SmartResume.git`（分支 `main`，需手动推送）

---

## 十二、待优化项

1. 一键优化选择「保留原样式」或「LaTeX」方案时，因无原始文件会自动回退到专业分栏 — 后续可增加从 JSON 直接生成 LaTeX/HTML 的能力
2. 当前仅支持单页 PDF 优化，多页 PDF 支持需增强