# 智能简历优化系统 — 功能概述

> 最后更新：2026-06-18

---

## 一、项目概述

智能简历优化系统是一个基于 AI 大语言模型的简历优化工具。用户上传简历和岗位描述后，系统自动解析、匹配分析、优化简历内容，并以多种排版方案输出 PDF。

**核心价值**：根据目标岗位要求，自动改写简历内容，提升匹配度，同时保持专业排版。

---

## 二、技术栈

### 后端

| 层级 | 技术 |
|------|------|
| 框架 | FastAPI (Python 3.11+) |
| 数据库 | PostgreSQL + SQLAlchemy (async) |
| 缓存/队列 | Redis (任务状态缓存 + 限流) |
| AI 模型 | OpenAI 兼容 API (默认 gpt-4o / gpt-4o-mini) |
| PDF 处理 | PyMuPDF (fitz)、WeasyPrint、Playwright、reportlab |
| LaTeX 编译 | xelatex (MiKTeX / TeX Live) |
| 文件存储 | 本地文件系统 (uploads/) |
| 认证 | JWT (access + refresh token) |
| 邮件 | SMTP (QQ邮箱) |

### 前端

| 层级 | 技术 |
|------|------|
| 框架 | Next.js 14 (App Router) |
| UI 组件库 | Ant Design 5 |
| 图表 | ECharts (仪表盘) |
| 文本对比 | diff (修改前后对比) |
| HTTP 客户端 | Axios |

---

## 三、系统架构

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Next.js)                     │
│  ┌──────────┬──────────┬──────────┬──────────────┐  │
│  │  首页     │ 简历库   │ 岗位库   │ 个人中心      │  │
│  │  (上传)   │          │          │              │  │
│  │  仪表盘   │ 历史记录 │ 历史详情 │ 管理员        │  │
│  └──────────┴──────────┴──────────┴──────────────┘  │
├─────────────────────────────────────────────────────┤
│                   FastAPI 后端                        │
│  ┌────────────────────────────────────────────────┐  │
│  │  API 路由层 (auth/jobs/resumes/optimize/...)   │  │
│  ├────────────────────────────────────────────────┤  │
│  │  服务层 (services/)                             │  │
│  │  resume_parser  job_parser  agent_optimizer    │  │
│  │  pdf_styler  pdf_generator  resume_renderer    │  │
│  │  scoring_engine  match_calculator  ...          │  │
│  ├────────────────────────────────────────────────┤  │
│  │  数据层 (models/)  PostgreSQL                   │  │
│  └────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

---

## 四、核心功能详解

### 4.1 用户认证与个人中心

- **注册/登录**：邮箱注册，JWT 双 token 认证
- **忘记密码**：邮箱验证码重置
- **个人中心**：
  - 编辑个人信息（昵称、头像、求职状态、求职意向）
  - 保存简历文本和岗位文本（`saved_texts` 字段）
  - 设备管理（多设备登录追踪）
  - 消息通知

### 4.2 简历管理

- **上传简历**：支持 PDF / Word / 图片（最大 20MB）
- **简历解析**：AI 解析为结构化 JSON（姓名、联系方式、工作经历、教育背景、技能、项目经历等）
- **简历库**：管理已上传简历，支持设为默认、软删除
- **从简历库选择**：优化时可直接从已有简历中选择

### 4.3 岗位管理

- **上传岗位截图**：支持 JPG / PNG / WebP
- **岗位解析**：AI 提取职位名称、公司、薪资、地点、必备技能、加分项等
- **岗位库**：管理已上传岗位，支持分类、收藏、设为默认
- **从岗位库选择**：优化时可直接从已有岗位中选择

### 4.4 简历优化（核心流程）

```
用户上传简历 + 岗位
       │
       ▼
┌─────────────┐
│ 1. 解析阶段  │  AI 解析简历文本 → parsed_json
│             │  AI 解析岗位描述 → parsed_job_json
└──────┬──────┘
       ▼
┌─────────────┐
│ 2. 匹配分析  │  AI 分析简历与岗位的匹配度
│             │  输出: match_score / strengths / gaps / rewrite_strategy
└──────┬──────┘
       ▼
┌─────────────┐
│ 3. 内容优化  │  AI 根据匹配分析结果改写简历
│             │  输出: 优化后的结构化 JSON
└──────┬──────┘
       ▼
┌─────────────┐
│ 4. 修改说明  │  AI 生成优化前后的差异说明
└──────┬──────┘
       ▼
┌─────────────┐
│ 5. PDF 生成  │  根据用户选择的方案生成排版 PDF
└─────────────┘
```

**两种优化入口**：

| 入口 | 说明 | 位置 |
|------|------|------|
| **文件上传** | 上传简历文件 + 岗位截图，再从简历库/岗位库选择 | 首页下方 |
| **一键优化** | 使用个人中心 `saved_texts` 中的简历文本和岗位文本直接优化 | 首页顶部蓝色卡片 |

**异步任务模式**：优化任务提交后立即返回 `task_id`，前端轮询 `/api/optimize/async/{task_id}` 获取进度和结果，最大等待 5 分钟。

### 4.5 四种 PDF 输出方案

| 方案 | 技术 | 说明 | 适用场景 |
|------|------|------|---------|
| **专业分栏** | Jinja2 + WeasyPrint | 左侧深蓝侧边栏，右侧主内容，A4 双栏 | 通用（上传/一键） |
| **简约单栏** | Jinja2 + WeasyPrint | 单栏居中，浅灰色调，简洁 | 通用（上传/一键） |
| **LaTeX 专业排版** | 视觉模型 → LaTeX → xelatex | 专业排版引擎，需安装 xelatex | 仅上传文件 |
| **保留原样式** | PyMuPDF 原地编辑 | 在原 PDF 上替换文字，保留矢量图形/字体/照片 | 仅 PDF 上传 |

> 用户可在「优化我的简历」按钮的下拉菜单中自由选择方案。一键优化（纯文本）选择不支持方案时自动回退到专业分栏。

### 4.6 优化结果展示

- **匹配度评分**：圆形进度条展示匹配分数
- **修改前后对比**：使用 `diff` 库进行文本级差异对比，绿色=新增，红色=删除
- **PDF 下载**：优化后的 PDF 可直接下载

### 4.7 历史记录

- 所有优化记录列表，支持按分类筛选
- 历史详情页：查看优化结果、匹配度、修改说明
- 支持对历史记录进行满意度评分和反馈
- **二次优化**：对已有优化结果再次优化（refine）

### 4.8 仪表盘

- 使用 ECharts 展示优化统计
- 优化次数、匹配度趋势等可视化数据

### 4.9 管理员功能

- 独立管理员登录
- 用户管理、系统监控

---

## 五、数据库模型关系

```
users (用户)
  ├── resumes (简历) 1:N
  ├── job_images (岗位) 1:N
  ├── optimized_resumes (优化记录) 1:N
  ├── user_devices (设备) 1:N
  ├── messages (消息) 1:N
  ├── user_quotas (配额) 1:1
  └── audit_logs (审计日志)

resumes ──→ optimized_resumes (1:N)
job_images ──→ optimized_resumes (1:N)
optimized_resumes ──→ optimized_resumes (自引用, refine 链)
```

---

## 六、API 路由总览

| 路由前缀 | 主要端点 | 功能 |
|----------|---------|------|
| `/api/auth` | register, login, refresh, logout | 认证 |
| `/api/user` | profile, update, sync-saved-texts | 用户信息 |
| `/api/resumes` | upload, list, delete, set-primary | 简历管理 |
| `/api/jobs` | upload, list, delete, set-primary, set-favorite | 岗位管理 |
| `/api/optimize` | async, async/{task_id}, quick, list, {id} | 简历优化 |
| `/api/feedback` | submit | 满意度反馈 |
| `/api/match` | analyze | 单独匹配分析 |
| `/api/admin` | users, stats | 管理后台 |
| `/api/messages` | list, read | 消息通知 |

---

## 七、AI 提示词模块

| 提示词文件 | 用途 |
|-----------|------|
| `resume_parse.py` | 将简历文本解析为结构化 JSON |
| `job_parse.py` | 将岗位描述解析为结构化 JSON |
| `match_analysis.py` | 分析简历与岗位的匹配度 |
| `optimize.py` | 根据匹配分析结果优化简历内容 |

---

## 八、配置项说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `DATABASE_URL` | PostgreSQL | 数据库连接 |
| `REDIS_URL` | localhost:6379 | Redis 连接 |
| `LLM_MODEL_VISION` | gpt-4o | 视觉模型（图片解析） |
| `LLM_MODEL_TEXT` | gpt-4o-mini | 文本模型（解析/优化） |
| `LLM_API_BASE` | OpenAI 官方 | 可自定义 API 地址 |
| `USE_WEASYPRINT` | true | 是否使用 WeasyPrint 渲染 |
| `USE_TEMPLATE_PDF` | true | 是否使用统一模板 |
| `MAX_UPLOAD_SIZE` | 20MB | 上传文件大小限制 |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | 120 | Token 过期时间 |

---

## 九、待优化项

1. 一键优化选择「保留原样式」或「LaTeX」方案时，因无原始文件会自动回退到专业分栏 — 后续可增加从 JSON 直接生成 LaTeX 的能力
2. 前端导航栏首次加载时 HTTP 请求可能阻塞渲染
3. 模板数量有限（目前 2 个 Jinja2 模板），可扩展更多风格模板