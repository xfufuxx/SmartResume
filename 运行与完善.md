# 运行与完善指南

> 更新日期：2026-05-24

---

## 1. 必须手动修改的配置（仅 `.env`）

编辑 [backend/.env](file:///d:/下载/智能简历/backend/.env)，修改以下两行：

| 变量 | 当前值 | 需要改成 | 原因 |
|------|--------|----------|------|
| `SECRET_KEY` | `your-secret-key-change-in-production` | 任意 32 位以上随机字符串 | 否则 JWT 可被伪造，不安全 |
| `LLM_API_KEY` | `sk-your-openai-api-key` | 你的真实 OpenAI API Key | 否则 AI 优化结果走固定 Mock 数据 |

示例：
```ini
SECRET_KEY=a3f8k2j9d0x1m4n7b2v5c8z1q6w3e0r9
LLM_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

其余配置**不需要动**。

---

## 2. 运行方式（二选一）

### 方式 A：Docker 一键启动（推荐，最省心）

在项目根目录打开终端，依次执行：

```bash
# 1. 构建并启动所有服务（数据库 + Redis + 后端 + 前端 + Celery）
docker-compose -f docker/docker-compose.yml up -d --build

# 2. 等待 30 秒左右，访问前端
#    http://localhost:3000

# 3. （可选）查看后端日志确认启动成功
docker-compose -f docker/docker-compose.yml logs backend
```

如果之前已经启动过，先停止再重新构建：
```bash
docker-compose -f docker/docker-compose.yml down
docker-compose -f docker/docker-compose.yml up -d --build
```

### 方式 B：本地逐步启动（适合开发调试）

需要 **4 个终端窗口**。

#### 前置步骤：修正数据库密码

docker-compose 中 PostgreSQL 密码是 `postgres`，但你的 `.env` 中写的是 `root`。如果用方式 B 运行，**先将 `.env` 的 `DATABASE_URL` 密码改为 `postgres`**：

```ini
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/smart_resume
```

#### 终端 1：启动 PostgreSQL + Redis
```bash
docker run -d --name postgres -e POSTGRES_DB=smart_resume -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16-alpine
docker run -d --name redis -p 6379:6379 redis:7-alpine
```

#### 终端 2：启动后端（FastAPI）
```bash
cd d:\下载\智能简历\backend
..\..venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

#### 终端 3：启动 Celery Worker（当前功能未使用，可选）
```bash
cd d:\下载\智能简历\backend
celery -A celery_worker.celery_app worker --loglevel=info --pool=solo
```

> Windows 下必须加 `--pool=solo`，否则 Celery 无法运行。

#### 终端 4：启动前端（Next.js）
```bash
cd d:\下载\智能简历\frontend
npm install
npm run dev
```

---

## 3. 运行后操作流程

| 步骤 | 操作 | 预期 |
|------|------|------|
| ① | 打开 http://localhost:3000 | 跳转到登录页 |
| ② | 点击「立即注册」 | 填写邮箱 + 密码（≥6 位） |
| ③ | 注册成功 | 自动登录并跳转首页 |
| ④ | 上传一份简历（PDF / Word / 图片） | 解析成功，显示基本信息 |
| ⑤ | 上传一张岗位需求截图（JPG / PNG） | 解析成功，显示职位/公司/薪资 |
| ⑥ | 点击「优化我的简历」 | 等待 AI 优化完成 |
| ⑦ | 查看优化结果 | 匹配分仪表盘 + 优势/差距 + 修改说明 |
| ⑧ | 点击顶部「历史记录」 | 查看所有简历/JD/优化记录，支持删除 |
| ⑨ | 点击「查看详情」 | 进入并排对比页面：原始 vs 优化内容逐段对比 |
| ⑩ | 点击「下载 PDF」 | 打开优化后的简历 PDF |

---

## 4. 服务端口一览

| 服务 | 端口 | 访问地址 |
|------|:---:|----------|
| 前端 | 3000 | http://localhost:3000 |
| 后端 API | 8000 | http://localhost:8000 |
| API 文档 | 8000 | http://localhost:8000/docs |
| PostgreSQL | 5432 | localhost:5432 |
| Redis | 6379 | localhost:6379 |

---

## 5. 当前项目还缺什么（不影响运行）

### 已完成功能清单
| 模块 | 状态 |
|------|:---:|
| 用户注册/登录/退出 | ✅ |
| 简历上传/解析/列表/删除 | ✅ |
| 岗位截图上传/解析/列表/删除 | ✅ |
| AI 匹配分析（匹配度 + 优势 + 差距） | ✅ |
| AI 简历优化 + PDF 生成 | ✅ |
| 优化历史记录列表 | ✅ |
| 优化详情（原始 vs 优化并排对比） | ✅ |
| 文件大小校验（20MB 限制） | ✅ |
| 响应式布局（移动端适配） | ✅ |
| 上传/解析加载状态 | ✅ |
| 错误信息精确提取 | ✅ |
| Docker 一键部署 + 健康检查 | ✅ |

### 尚未实现的部分

| 类别 | 说明 | 优先级 | 影响 |
|------|------|:---:|------|
| **LLM 密钥** | 没有真实 Key 时走 Mock 数据，结果固定 | **P0** | AI 优化无真实效果 |
| **数据库迁移文件** | 首次运行 `create_all` 自动建表够用，后续改表结构需要 alembic | P2 | 生产环境不够规范 |
| **测试文件** | 无 pytest（后端）/ Jest（前端），无法自动化回归测试 | P3 | 无法 CI/CD 验证 |
| **密码强度校验** | 后端只要求 ≥6 位，无复杂度约束 | P3 | 用户可能设弱密码 |
| **解析结果手动编辑** | 解析后无法在页面中修改字段再提交优化 | P2 | spec 承诺功能 |
| **异步优化 + 进度推送** | 目前同步等待 LLM 返回（大文件可能超时） | P1 | 核心体验 |
| **WebSocket 实时进度** | 优化过程无进度条推送 | P2 | 体验优化 |
| **Ctrl+V 粘贴截图** | 只支持拖拽/点击上传 | P2 | 体验优化 |
| **CI/CD 流水线** | 无 GitHub Actions / GitLab CI | P3 | 工程化 |
| **APM / 错误追踪** | 无 Sentry 等日志追踪 | P3 | 运维可观测性 |

### 建议的下一步（优先级排序）

1. **P0**：填入真实 `LLM_API_KEY` → 体验真正的 AI 优化效果
2. **P1**：将同步优化改为异步任务 + 前端轮询状态 → 避免 HTTP 超时
3. **P2**：添加解析结果手动编辑功能 → 补全 spec 承诺的交互
4. **P2**：补充 alembic 迁移文件 → `cd backend && alembic revision --autogenerate -m "init"`

---

## 6. 常见问题

| 问题 | 解决方法 |
|------|----------|
| 注册成功但数据库没数据 | 已修复：commit 时机问题已解决，重启后端即可 |
| 登录报 UnknownHashError | 已修复：密码算法兼容 bcrypt + bcrypt_sha256 两种格式 |
| Docker 启动后端报错 DB 连接失败 | 检查 `.env` 中 `DATABASE_URL` 密码，docker-compose 中覆盖为 `postgres` |
| 前端上传文件 401 | 确保已注册登录，localStorage 中有 token |
| 优化结果一直是 Mock 数据 | 检查 `LLM_API_KEY` 是否已改为真实 Key |
