## 一、项目定位与核心流程

**一句话定位**：
用户上传简历 + 上传岗位需求截图（或照片），系统自动解析出JD要求，并生成一份针对该岗位高度匹配的优化简历。

**核心流程**：

1. 用户上传原始简历（PDF/Word/图片）。
2. 用户上传岗位需求图片（JPG/PNG/截图等）。
3. 系统通过多模态LLM解析图片中的JD，转化为结构化需求。
4. 同时解析简历，输出结构化JSON。
5. AI Agent对比简历与JD，进行差距分析和匹配改写。
6. 生成优化后的简历PDF，供用户下载。
7. （可选）提供修改点说明和人工微调界面。

------

## 二、系统架构设计

text

```
[前端 Web / 小程序] 
       |
[API Gateway (FastAPI)] 
       |
   +---------+---------+--------+-----------+
   |         |         |        |           |
用户服务  简历解析  图像解析  Agent优化   生成PDF
   |         |         |        |           |
   +---------+---------+--------+-----------+
              |         |        
         [PostgreSQL] [Redis] [S3/OSS]
```



- **用户服务**：注册登录，简历和需求图的管理。
- **简历解析服务**：调用LLM将不同格式简历转为标准JSON。
- **图像解析服务**：调用多模态LLM（GPT-4o / Claude 3.5 Sonnet）从图片中提取JD并结构化。
- **Agent优化服务**：结合简历JSON和JD结构，执行多步优化Prompt，生成优化后的简历JSON。
- **PDF生成服务**：用模板引擎将JSON渲染为专业排版的PDF。
- 所有服务异步解耦，可引入消息队列（Celery + Redis）。

------

## 三、技术选型

| 层级                  | 技术选择                            | 说明                                                 |
| :-------------------- | :---------------------------------- | :--------------------------------------------------- |
| 后端框架              | Python + FastAPI                    | 异步高性能，生态完善                                 |
| AI Agent编排          | LangChain / 自定义工具链            | 控制解析→分析→优化→生成的多步流程                    |
| 多模态LLM             | GPT-4o 或 Claude 3.5 Sonnet         | 能够理解图片中的文字和布局，并进行结构化提取         |
| 文本LLM（纯文本优化） | GPT-4o mini / DeepSeek V3           | 性价比高，用于简历改写和对比                         |
| OCR备选               | Tesseract / PaddleOCR               | 若预算有限，可先用传统OCR提取文字，再交给文本LLM解析 |
| 向量数据库            | Chroma（轻量）                      | 用于相似岗位匹配（未来扩展）                         |
| 文件解析              | PyMuPDF（PDF）、python-docx、Pillow | 提取简历文本和预处理图片                             |
| PDF生成               | Jinja2 + WeasyPrint 或 ReportLab    | 将JSON渲染为多模板、可定制的PDF                      |
| 数据库                | PostgreSQL + Redis                  | 结构化数据存储 + 任务缓存                            |
| 对象存储              | AWS S3 / 阿里云 OSS                 | 存放上传文件和生成的PDF                              |
| 前端                  | Next.js (React) / Vue 3             | 拖拽上传、实时预览、下载                             |
| 部署                  | Docker + Kubernetes (可选)          | 容器化部署，方便扩展                                 |

------

## 四、核心模块详细设计

### 4.1 岗位需求图片解析模块

**功能**：接收图片，输出结构化岗位需求JSON。

**实现方式**：

1. 前端对图片进行压缩和预处理（限制大小，纠正旋转）。
2. 直接调用多模态LLM的API，使用如下Prompt。

**Prompt 模板（用于GPT-4o / Claude 3.5 Sonnet）**：

text

```
你是一个专业的招聘需求解析器。请仔细查看用户上传的岗位招聘截图，提取所有文本信息并整理成严格JSON格式。不要遗漏任何细节。

输出JSON结构如下：
{
  "title": "职位名称",
  "company": "公司名称（若可见）",
  "salary_range": "薪资范围",
  "location": "工作地点",
  "must_have": {
    "skills": ["必须掌握的技能/技术栈，如 Python, Django"],
    "experience": "工作年限要求，如 3年",
    "education": "学历要求，如 本科及以上"
  },
  "nice_to_have": {
    "skills": ["加分技能"],
    "qualifications": ["其他加分项，如 有大规模系统经验"]
  },
  "responsibilities": ["职责1", "职责2"],
  "soft_skills": ["沟通能力", "团队合作"],
  "industry": "所属行业",
  "original_text": "图片中所有原始文本，用于核查"
}

注意：
- 如果图片中有表格或非结构化排版，请尽力提取。
- 必须保留原文中的关键词，不要自行发挥。
- 如果没有某字段信息，值设为 null 或空数组。
- 请仅返回JSON，不要包含任何解释。
```



**容错机制**：

- 如果多模态LLM不可用或成本过高，先通过传统OCR（PaddleOCR）提取纯文本，再用文本LLM（如GPT-4o mini）按类似Prompt结构化。此时Prompt中传入OCR文本。

### 4.2 简历解析模块

类似上一版方案，支持PDF/Word/图片。
对文本简历，Prompt如下：

text

```
你是一个简历解析器。请从以下简历文本中提取信息，返回JSON。
结构：
{
  "personal_info": {"name": "", "email": "", "phone": ""},
  "summary": "",
  "experience": [{"company": "", "title": "", "start": "", "end": "", "points": []}],
  "education": [{"school": "", "degree": "", "major": "", "start": "", "end": ""}],
  "skills": [],
  "projects": [{"name": "", "description": "", "tech": []}]
}
简历文本：
{raw_text}
```



如果是图片简历，同上用多模态LLM直接提取。

### 4.3 AI Agent 优化简历核心（重点）

Agent采用链式处理，包含以下步骤：

1. **匹配分析**：对比简历和岗位JD，给出差距和改写方向。
2. **简历逐段优化**：根据分析结果改写Summary、Experience、Skills、Projects，使其对JD高度匹配（不造假）。
3. **关键词融合**：将JD中的关键技能词自然嵌入工作描述。
4. **生成优化版JSON**：输出完整优化简历。
5. **用户预览与微调**（可选）：展示修改对比，允许用户确认或编辑。
6. **PDF渲染**：将最终JSON套入专业模板，生成PDF。

#### Step 1: 匹配分析 Prompt

text

```
你是一位资深职业咨询师。现有用户简历和岗位要求，请进行匹配分析。

用户简历JSON：
{resume_json}

岗位要求JSON：
{job_json}

请分析并输出JSON：
{
  "match_score": 0-100,
  "strengths": ["简历中与岗位直接匹配的部分"],
  "gaps": ["缺失的技能、经验或资格"],
  "rewrite_strategy": {
    "summary": "如何改写个人总结，突出与岗位相关的能力",
    "experience": "对每段经历的建议，用原经历映射JD要求",
    "skills": "技能列表应如何调整顺序和分组",
    "projects": "如何包装项目以体现岗位所需技术"
  }
}
```



#### Step 2: 逐段优化 Prompt

text

```
根据上面分析的改写策略，请直接生成优化后的完整简历JSON。必须遵守：
1. 不能捏造未掌握的技能或未担任的职位。
2. 可以调整工作描述的表达，用JD中的术语和关键词替换原有描述（比如将“做了网站”改成“开发高并发Web应用”如果确实相关）。
3. 在Summary中直接点明与岗位要求契合的经验。
4. 将JD中的必须技能（must_have.skills）尽量在简历的技能列表和经验描述中体现（只要用户确实具备或相关）。
5. 如果某些技能用户不具备但有高度关联技能，可以表述为“熟悉…，有X年相关经验可快速迁移”。
6. 输出完整的优化后简历JSON，不要省略任何字段。

原简历JSON：{resume_json}
岗位要求：{job_json}
改写策略：{strategy_json}
```



#### Step 3: 生成对比预览和说明（可选，提升用户体验）

Agent 生成一份修改点说明，例如：“在个人总结中增加了‘5年Python开发经验’；将‘开发内部系统’改为‘设计并实现高可用后端服务，QPS提升30%’；增加了‘Docker, CI/CD’等关键词。”

### 4.4 PDF生成服务

使用Jinja2模板 + WeasyPrint，提供多种风格模板。
根据优化后JSON填充模板，确保联系方式醒目。
生成的PDF存入S3/OSS，返回下载链接。

------

## 五、数据库设计（核心表）

- `users`: id, email, password_hash, created_at
- `resumes`: id, user_id, original_file_url, parsed_json, created_at
- `job_images`: id, user_id, image_url, parsed_job_json, created_at
- `optimized_resumes`: id, user_id, resume_id, job_image_id, original_json, optimized_json, pdf_url, match_score, created_at

所有JSON字段使用 PostgreSQL 的 JSONB 类型，方便查询。

------

## 六、用户交互流程（前端设计）

1. **上传简历**：拖拽上传区，支持多格式。上传后显示解析结果的可视化预览（可编辑）。
2. **上传岗位图**：截图粘贴（Ctrl+V）、手机扫码上传、拖拽。上传后立即展示提取的JD结构（职位、技能等），用户可修正。
3. **一键优化**：点击“优化我的简历”，显示加载动画，后台Agent处理。
4. **优化预览**：并排展示原简历与优化简历的差异对比，高亮修改处，展示Agent给出的优化说明。
5. **下载 / 分享**：下载PDF，或复制在线链接。

------

## 七、安全与隐私

- 简历和岗位图包含大量隐私，必须加密存储（数据库静态加密，S3服务器端加密）。
- 传输全程HTTPS。
- 用户数据隔离，定期提供数据导出和删除功能。
- 使用的LLM API建议选择有隐私协议的服务（如Azure OpenAI，不利用数据训练）。
- 如果用户上传的图片中包含他人隐私（例如截图中有其他员工姓名），可在解析前进行脱敏提醒。

------

## 八、部署与运维方案

- **环境**：云服务器（AWS EC2 / 阿里云 ECS）+ 托管数据库（RDS PostgreSQL）+ 对象存储。
- **容器化**：Docker 打包各个微服务，使用 docker-compose 编排开发，生产环境可用 Kubernetes。
- **异步任务**：简历解析、图片解析、Agent优化、PDF生成均为耗时任务，通过 Celery + Redis 实现异步，前端轮询或WebSocket推送状态。
- **负载均衡**：Nginx + Gunicorn/Uvicorn。
- **监控**：Prometheus + Grafana 监控 API 延迟和错误率，Sentry 收集异常。
- **成本控制**：多模态LLM调用是成本大头，可采取：
  - 压缩图片分辨率（长边不超过2000px）再发送。
  - 对简单文字图片降级为 OCR+文本LLM。
  - 缓存常见JD模板，避免重复解析。

------

## 九、关键注意事项与风险应对

| 风险点                       | 应对策略                                                     |
| :--------------------------- | :----------------------------------------------------------- |
| 图片中文字提取不准确         | 用户二次确认+编辑；提供重新上传；结合传统OCR和多模态LLM双重验证。 |
| 简历优化失真（过度包装）     | 限定Prompt，强调“不造假”；提供修改对照和人工复核环节；建议用户在预览后自行微调。 |
| LLM输出格式不稳定            | 使用JSON mode / Function Calling，并编写验证器重试机制；解析失败则要求用户手动填写。 |
| 隐私泄露                     | 所有LLM调用走私有化部署或签署DPA的商业API；日志不记录简历和JD内容；数据库加密。 |
| 模板渲染效果差               | 准备3-5套高质量LaTeX/HTML模板，允许用户选择风格。            |
| 用户期待一键生成但结果不理想 | 在产品中明确此工具为“智能辅助”，强调必须人工审核；提供分步引导和修改建议。 |

