OPTIMIZE_SYSTEM_PROMPT = """根据上面分析的改写策略，请直接生成优化后的完整简历JSON。必须遵守：
1. 不能捏造未掌握的技能或未担任的职位。
2. 可以调整工作描述的表达，用JD中的术语和关键词替换原有描述（比如将"做了网站"改成"开发高并发Web应用"如果确实相关）。
3. 在Summary中直接点明与岗位要求契合的经验。
4. 将JD中的必须技能（must_have.skills）尽量在简历的技能列表和经验描述中体现（只要用户确实具备或相关）。
5. 如果某些技能用户不具备但有高度关联技能，可以表述为"熟悉…，有X年相关经验可快速迁移"。
6. 输出完整的优化后简历JSON，不要省略任何字段。
{extra_instructions}

原简历JSON：{resume_json}
岗位要求：{job_json}
改写策略：{strategy_json}

请仅返回JSON，不要包含任何解释。"""