# 智能简历 — 项目功能总结

智能简历是一个基于 AI 的全栈简历优化平台，后端 **FastAPI + PostgreSQL + Redis**，前端 **Next.js 14 + Ant Design 5**。

---

## 用户端功能

| 模块 | 功能要点 |
|------|----------|
| **认证与账户** | 邮箱注册/登录、忘记/修改密码、SMTP 验证码、绑定手机、账号注销、JWT 双令牌、多设备管理、隐私协议、数据导出 |
| **简历管理** | 上传（PDF/DOCX/图片）→ AI 解析 → 多简历库 → 软删除回收 |
| **职位管理** | 上传招聘截图 → OCR 识别 → AI 提取岗位要求/技能/职责 |
| **AI 简历优化（核心）** | 简历+职位匹配分析 → AI 重写优化 → 精炼（Refine）→ 版本对比（Diff）→ 收藏/回收站 → PDF 导出 → 满意度评分 |
| **简历评分** | AI 综合评分 + 四维分析（完整性/关键词/量化/格式）+ 改进建议 |
| **岗位匹配** | 匹配百分比 + 缺失关键词检测 |
| **批量优化** | 一份简历同时匹配多个职位，实时查看进度 |
| **面试反馈追踪** | 记录面试结果，统计面试率/Offer 率，跨版本效果对比 |
| **消息通知** | 系统消息列表、标记已读、删除 |
| **个人中心** | 个人信息/职业期望编辑、头像上传、额度查看 |

---

## 管理后台功能

| 模块 | 功能要点 |
|------|----------|
| **管理员认证** | 独立登录页、JWT 鉴权、操作自动记录 |
| **数据大屏** | 核心指标（DAU/优化数/付费率）、行业占比饼图、匹配度趋势折线图、功能使用柱状图、热门岗位排行、满意度趋势，30s 自动刷新 |
| **用户管理** | 列表搜索筛选、详情查看、封禁/解封、VIP 设置、额度调整 |
| **内容管理** | 简历模板 CRUD、行业关键词管理（含批量导入）、ATS 筛选规则配置 |
| **AI 模型管理** | Prompt 模板多场景管理、版本控制+灰度发布+回滚、模型路由权重/限流/熔断、调用日志查询 |
| **任务监控** | 今日统计/成功率/延迟、失败原因分布、模型健康度、失败重试 |
| **财务管理** | 订单查询/退款（30天期）、营收趋势图、套餐管理 |
| **反馈工单** | 满意度反馈筛选与聚类、客服工单回复与状态流转（支持内部备注）|
| **审计日志** | 管理员操作日志查询 |

---

## 技术栈

| 层 | 技术 |
|----|------|
| 后端 | FastAPI / SQLAlchemy 2.0 async / asyncpg / PostgreSQL / Redis |
| AI | OpenAI 兼容 API / Prompt 模板 + 灰度 + 模型路由 + 熔断 |
| 前端 | Next.js 14 App Router / Ant Design 5 / ECharts 5 / Axios |
| 存储 | S3 兼容 / 本地存储 |
| 认证 | JWT（Access + Refresh）/ passlib bcrypt |
| 邮件 | smtplib + QQ邮箱 SMTP |

## 数据库（15+ 张表）

用户类：`users` `user_devices` `user_quotas` `verification_codes`
业务类：`resumes` `optimized_resumes` `job_images` `messages` `audit_logs` `feedbacks`
管理类：`admins` `admin_logs` `prompt_templates` `model_routing` `ai_model_call_logs`
内容类：`resume_templates` `industry_keywords` `ats_rules`
财务类：`orders` `quota_packages`
工单类：`support_tickets` `ticket_replies`

## API 端点（60+）

`/api/auth` 认证 · `/api/user` 用户 · `/api/messages` 消息 · `/api/resumes` 简历 · `/api/jobs` 职位 · `/api/optimize` 优化 · `/api/match` 匹配 · `/api/feedback` 反馈 · `/api/batch` 批量 · `/api/refine` 精炼 · `/api/resume/*/score` 评分 · `/api/admin` 管理后台

---

> 最后更新：2026-06-02